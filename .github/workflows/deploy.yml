name: Deploy Error Debugger

on:
  push:
    branches: [main]
    paths:
      - 'agent/**'
      - 'app/**'
      - 'terraform/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        default: 'deploy'
        type: choice
        options:
          - deploy
          - destroy
      feature_part:
        description: 'Blog Series Part (1=basic, 2=advanced with memory/github)'
        required: true
        default: '2'
        type: choice
        options:
          - '1'
          - '2'

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
  PROJECT_NAME: ${{ vars.PROJECT_NAME || 'error-debugger' }}
  ENVIRONMENT: ${{ vars.ENVIRONMENT || 'prod' }}
  FEATURE_PART: ${{ github.event.inputs.feature_part || '2' }}
  TF_VAR_project_name: ${{ vars.PROJECT_NAME || 'error-debugger' }}
  TF_VAR_environment: ${{ vars.ENVIRONMENT || 'prod' }}
  TF_VAR_aws_region: ${{ vars.AWS_REGION || 'us-east-1' }}
  TF_VAR_feature_part: ${{ github.event.inputs.feature_part || '2' }}
  # API tokens for external services (optional but recommended)
  TF_VAR_github_token: ${{ secrets.GH_PAT }}
  TF_VAR_stackoverflow_api_key: ${{ secrets.STACKOVERFLOW_API_KEY }}

jobs:
  deploy:
    name: Deploy Infrastructure
    runs-on: ubuntu-latest
    if: github.event.inputs.action != 'destroy'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0
          terraform_wrapper: false
      
      # ===== Get Backend Bucket Name =====
      - name: Get Terraform Backend
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          BUCKET_NAME="${PROJECT_NAME}-terraform-state-${ACCOUNT_ID}"
          echo "TF_BACKEND_BUCKET=${BUCKET_NAME}" >> $GITHUB_ENV
          echo "Using backend bucket: $BUCKET_NAME"
      
      # ===== Build & Push Container =====
      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Set up QEMU (for ARM64)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64
      
      - name: Build & Push Agent Container
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          REPO_URL="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${PROJECT_NAME}-${ENVIRONMENT}-agent"
          
          echo "🐳 Building multi-agent container for ARM64..."
          cd agent
          docker buildx build \
            --platform linux/arm64 \
            --push \
            -t ${REPO_URL}:latest \
            -t ${REPO_URL}:${{ github.sha }} \
            .
          
          echo "CONTAINER_IMAGE=${REPO_URL}:latest" >> $GITHUB_ENV
      
      # ===== Terraform =====
      - name: Terraform Init
        working-directory: terraform/agentcore
        run: terraform init -upgrade -backend-config="bucket=${{ env.TF_BACKEND_BUCKET }}"
      
      - name: Terraform Plan
        working-directory: terraform/agentcore
        run: terraform plan -out=tfplan
      
      - name: Terraform Apply
        working-directory: terraform/agentcore
        run: terraform apply -auto-approve tfplan
      
      # ===== Get Outputs =====
      - name: Get Terraform Outputs
        id: tf-outputs
        working-directory: terraform/agentcore
        run: |
          echo "ecr_repository=$(terraform output -raw ecr_repository_url)" >> $GITHUB_OUTPUT
          echo "s3_bucket=$(terraform output -raw s3_bucket_name)" >> $GITHUB_OUTPUT
          echo "cloudfront_id=$(terraform output -raw cloudfront_distribution_id)" >> $GITHUB_OUTPUT
          echo "runtime_id=$(terraform output -raw runtime_id)" >> $GITHUB_OUTPUT
          echo "cognito_pool_id=$(terraform output -raw cognito_identity_pool_id)" >> $GITHUB_OUTPUT
          echo "website_url=$(terraform output -raw website_url)" >> $GITHUB_OUTPUT
          
          # Get full frontend config as JSON
          FRONTEND_CONFIG=$(terraform output -json frontend_config)
          echo "frontend_config<<EOFCONFIG" >> $GITHUB_OUTPUT
          echo "$FRONTEND_CONFIG" >> $GITHUB_OUTPUT
          echo "EOFCONFIG" >> $GITHUB_OUTPUT
      
      # ===== Deploy Frontend =====
      - name: Deploy Frontend
        run: |
          # Create runtime config from Terraform frontend_config output
          cat > app/config.js << 'EOFJS'
          window.AGENTCORE_CONFIG = ${{ steps.tf-outputs.outputs.frontend_config }};
          EOFJS
          
          echo "📦 Generated config.js:"
          cat app/config.js
          echo ""
          echo "🎯 Deploying Part ${{ env.FEATURE_PART }} features"
          
          # Sync static assets with long cache
          aws s3 sync app/ s3://${{ steps.tf-outputs.outputs.s3_bucket }}/ \
            --delete \
            --cache-control "public, max-age=31536000" \
            --exclude "*.html" \
            --exclude "config.js"
          
          # Upload HTML and config with no-cache
          aws s3 cp app/index.html s3://${{ steps.tf-outputs.outputs.s3_bucket }}/ \
            --cache-control "no-cache, no-store, must-revalidate"
          
          aws s3 cp app/config.js s3://${{ steps.tf-outputs.outputs.s3_bucket }}/ \
            --cache-control "no-cache, no-store, must-revalidate"
      
      # ===== Invalidate CloudFront =====
      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ steps.tf-outputs.outputs.cloudfront_id }} \
            --paths "/*"
      
      # ===== Summary =====
      - name: Deployment Summary
        run: |
          echo "## 🐛 Error Debugger Deployed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ env.FEATURE_PART }}" == "1" ]; then
            echo "### 📝 Part 1: Basic Multi-Agent System" >> $GITHUB_STEP_SUMMARY
            echo "5 agents (Parser, Security, Root Cause, Fix, Supervisor) | Lambda tools | Bedrock" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 📝 Part 2: Advanced Features" >> $GITHUB_STEP_SUMMARY
            echo "7 agents + Memory + GitHub Integration + Live Visualization" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### AgentCore Resources" >> $GITHUB_STEP_SUMMARY
          echo "| Resource | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Runtime | \`${{ steps.tf-outputs.outputs.runtime_id }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Cognito Pool | \`${{ steps.tf-outputs.outputs.cognito_pool_id }}\` |" >> $GITHUB_STEP_SUMMARY
          if [ "${{ env.FEATURE_PART }}" == "2" ]; then
            echo "| Memory | \`${{ steps.tf-outputs.outputs.memory_id }}\` |" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🌐 Website" >> $GITHUB_STEP_SUMMARY
          echo "[${{ steps.tf-outputs.outputs.website_url }}](${{ steps.tf-outputs.outputs.website_url }})" >> $GITHUB_STEP_SUMMARY

  destroy:
    name: Destroy Infrastructure
    runs-on: ubuntu-latest
    if: github.event.inputs.action == 'destroy'
    environment: destroy
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0
      
      - name: Get Terraform Backend
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          BUCKET_NAME="${PROJECT_NAME}-terraform-state-${ACCOUNT_ID}"
          echo "TF_BACKEND_BUCKET=${BUCKET_NAME}" >> $GITHUB_ENV
      
      - name: Terraform Init
        working-directory: terraform/agentcore
        run: terraform init -backend-config="bucket=${{ env.TF_BACKEND_BUCKET }}"
      
      - name: Empty S3 Bucket
        run: |
          cd terraform/agentcore
          BUCKET=$(terraform output -raw s3_bucket_name 2>/dev/null || echo "")
          if [ -n "$BUCKET" ]; then
            echo "Emptying bucket: $BUCKET"
            aws s3 rm s3://$BUCKET --recursive || true
          fi
      
      - name: Terraform Destroy
        working-directory: terraform/agentcore
        run: terraform destroy -auto-approve
      
      - name: Summary
        run: |
          echo "## 🗑️ Infrastructure Destroyed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All AgentCore resources have been removed." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Note:** Bootstrap resources (S3 state bucket, DynamoDB lock table, ECR) are preserved." >> $GITHUB_STEP_SUMMARY
          echo "Run the **Bootstrap** workflow with 'destroy' to remove those as well." >> $GITHUB_STEP_SUMMARY

